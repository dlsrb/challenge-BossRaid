Mediator는 판단(해석/결정)만 한다.

입력: Requested 이벤트 / Occurred 이벤트

출력: Decision 이벤트 / Command(Request) 이벤트

직접 실행 금지: Player/Boss 메서드 호출, Unity 컴포넌트(Transform/Animator/HP) 조작 금지

Event Layer 침범 금지: EventBus/Logger 정책 변경 금지

상태 보관은 판정에 필요한 최소만 유지(UI/연출 상태 금지)